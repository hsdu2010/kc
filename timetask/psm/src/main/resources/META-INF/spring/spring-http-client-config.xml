<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
           ">

	<!-- httpclient 连接池（默认只需1个） -->
	<bean id="poolingHttpClientConnectionManager" class="shade.storm.org.apache.http.impl.conn.PoolingHttpClientConnectionManager">
		<property name="maxTotal" value="${httpclient.pool.maxTotal:20}" />
		<property name="defaultMaxPerRoute" value="${httpclient.pool.defaultMaxPerRoute:5}" />
	</bean>

	<!-- 机构信息配置，httpclient 连接路由 （可根据连接的主机配置多个，同一个ip:port只需1个） -->
	<bean id="departmentCfgHttpClient" class="com.sf.shiva.oms.psm.common.utils.PooledHttpClient">
		<property name="ip" value="${http.client.department.cfg.ip:null}" />
		<property name="port" value="${http.client.department.cfg.port:null}" />
		<property name="maxRouteConn" value="${http.client.department.cfg.maxRouteConn:5}" />
		<property name="socketTimeout" value="${http.client.department.cfg.socketTimeout:3000}" />
		<property name="connectTimeout" value="${http.client.department.cfg.connectTimeout:3000}" />
		<property name="connectionRequestTimeout" value="${http.client.department.cfg.connectionRequestTimeout:10000}" />
	</bean>
	
	<!-- 号段配置信息查询配置 -->
	<bean id="nsCfgHttpClient" class="com.sf.shiva.oms.psm.common.utils.PooledHttpClient">
		<property name="ip" value="${http.client.nsCfg.cfg.ip:null}" />
		<property name="port" value="${http.client.nsCfg.cfg.port:null}" />
		<property name="maxRouteConn" value="${http.client.nsCfg.cfg.maxRouteConn:5}" />
		<property name="socketTimeout" value="${http.client.nsCfg.cfg.socketTimeout:3000}" />
		<property name="connectTimeout" value="${http.client.nsCfg.cfg.connectTimeout:3000}" />
		<property name="connectionRequestTimeout" value="${http.client.nsCfg.cfg.connectionRequestTimeout:10000}" />
	</bean>
</beans>